# AQI Java Backend

Spring Boot backend exposing `/api/aqi?city=CityName`.

## Run

```bash
cd backend-java
mvn spring-boot:run
```

Edit `src/main/resources/application.properties` and set `aqicn.api.token` to your real token.